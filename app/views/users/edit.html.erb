<% provide(:title, 'Edit user') %>
<% provide(:button_text, 'Save changes') %>


<div class="form-card">
    <h1>Update your profile</h1>
    <div>
        <%= render 'shared/form' %>
        <div class="gravatar_edit form-card">
            <%= gravatar_for @user %>
            <a href="https://gravatar.com/emails" target="_blank">Change</a>
        </div>
    </div>
</div>